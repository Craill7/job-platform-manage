<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.recruit.mapper.UsersMapper">
    
    <resultMap type="Users" id="UsersResult">
        <result property="id"    column="id"    />
        <result property="email"    column="email"    />
        <result property="passwordHash"    column="password_hash"    />
        <result property="role"    column="role"    />
        <result property="status"    column="status"    />
        <result property="createdAt"    column="created_at"    />
        <result property="updatedAt"    column="updated_at"    />
        <result property="lastLoginAt"    column="last_login_at"    />
    </resultMap>

    <resultMap type="com.ruoyi.recruit.domain.CompanyAudit" id="CompanyAuditResult">
        <result property="userId"       column="id"    />
        <result property="email"        column="email"    />
        <result property="role"         column="role"    />
        <result property="status"       column="status"    />
        <result property="createdAt"    column="created_at"    />
        <result property="companyName"  column="company_name"    />
        <result property="auditedAt"    column="audited_at"    />
    </resultMap>

    <sql id="selectUsersVo">
        select id, email, password_hash, role, status, created_at, updated_at, last_login_at from users
    </sql>

    <select id="selectUsersList" parameterType="Users" resultMap="UsersResult">
        <include refid="selectUsersVo"/>
        <where>  
            <if test="id != null "> and id = #{id}</if>
            <if test="email != null  and email != ''"> and email like concat('%', #{email}, '%')</if>
            <if test="role != null "> and role = #{role}</if>
            <if test="status != null "> and status = #{status}</if>
            <if test="params.beginCreatedAt != null and params.beginCreatedAt != '' and params.endCreatedAt != null and params.endCreatedAt != ''"> and created_at >= #{params.beginCreatedAt} and created_at &lt; DATE_ADD(#{params.endCreatedAt}, INTERVAL 1 DAY)</if>
        </where>
    </select>
    
    <select id="selectUsersById" parameterType="Long" resultMap="UsersResult">
        <include refid="selectUsersVo"/>
        where id = #{id}
    </select>

    <select id="selectCompanyAuditList" parameterType="com.ruoyi.recruit.domain.Users" resultMap="CompanyAuditResult">
        SELECT
        u.id,
        u.email,
        u.role,
        u.status,
        u.created_at,
        c.company_name,
        CASE WHEN u.status = 0 THEN NULL ELSE u.updated_at END as audited_at
        FROM users u
        LEFT JOIN companies c ON u.id = c.user_id
        <where>
            AND u.role = 2  <if test="email != null and email != ''">
            AND u.email LIKE concat('%', #{email}, '%')
        </if>
            <if test="status != null">
                AND u.status = #{status}
            </if>
            <if test="params.companyName != null and params.companyName != ''">
                AND c.company_name LIKE concat('%', #{params.companyName}, '%')
            </if>
            <if test="params.beginCreatedAt != null and params.beginCreatedAt != ''">
                AND u.created_at &gt;= #{params.beginCreatedAt}
            </if>
            <if test="params.endCreatedAt != null and params.endCreatedAt != ''">
                AND u.created_at &lt;= #{params.endCreatedAt}
            </if>
        </where>
        ORDER BY u.created_at DESC
    </select>

    <select id="selectCompanyAuditById" parameterType="Long" resultMap="CompanyAuditResult">
        SELECT
            u.id, u.email, u.role, u.status, u.created_at,
            c.company_name,
            CASE WHEN u.status = 0 THEN NULL ELSE u.updated_at END as audited_at
        FROM users u
                 LEFT JOIN companies c ON u.id = c.user_id
        WHERE u.id = #{id}
    </select>

    <select id="countPendingCompanies" resultType="int">
        select count(1) from users where role = 2 and status = 0
    </select>

    <insert id="insertUsers" parameterType="Users" useGeneratedKeys="true" keyProperty="id">
        insert into users
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="email != null">email,</if>
            password_hash,
            <if test="role != null">role,</if>
            <if test="status != null">status,</if>
            <if test="createdAt != null">created_at,</if>
            <if test="updatedAt != null">updated_at,</if>
            <if test="lastLoginAt != null">last_login_at,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="email != null">#{email},</if>
            #{passwordHash},
            <if test="role != null">#{role},</if>
            <if test="status != null">#{status},</if>
            <if test="createdAt != null">#{createdAt},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
            <if test="lastLoginAt != null">#{lastLoginAt},</if>
         </trim>
    </insert>

    <update id="updateUsers" parameterType="Users">
        update users
        <trim prefix="SET" suffixOverrides=",">
            <if test="email != null">email = #{email},</if>
            <if test="passwordHash != null">password_hash = #{passwordHash},</if>
            <if test="role != null">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
            <if test="lastLoginAt != null">last_login_at = #{lastLoginAt},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteUsersById" parameterType="Long">
        delete from users where id = #{id}
    </delete>

    <delete id="deleteUsersByIds" parameterType="String">
        delete from users where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>